package fr.supercomete.head.permissions;

public enum Permissions {
    Allow_config,
    Allow_bypass,
    Allow_hsay,
    Allow_add_whitelist,
    Allow_koff,
    Allow_groups_message,
    Allow_setgroup,
    Allow_change_game_name,
    Allow_force,
    Allow_finalheal,
    See_helpop
}